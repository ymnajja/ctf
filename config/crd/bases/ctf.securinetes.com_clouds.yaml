---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: clouds.ctf.securinetes.com
spec:
  group: ctf.securinetes.com
  names:
    kind: Cloud
    listKind: CloudList
    plural: clouds
    singular: cloud
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Cloud is the Schema for the clouds API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudSpec defines the desired state of Cloud
            properties:
              escape:
                description: Foo is an example field of Cloud. Edit cloud_types.go
                  to remove/update
                type: boolean
            type: object
          status:
            description: CloudStatus defines the observed state of Cloud
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
